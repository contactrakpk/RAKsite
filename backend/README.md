# RAK Commerce API

Run locally:

```bash
npm install
copy .env.example .env
npm start
```

Before starting, create PostgreSQL tables with `schema.sql`, set `DATABASE_URL`, generate a bcrypt password hash, and insert an admin row. Never commit `.env`.
